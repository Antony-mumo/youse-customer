// use this file to import custom fonts, added in .ttf formart
// save the font bile in the ./src/assets/fonts/

module.exports = {
    assets:['./src/assets/fonts/'],
  };

// remember After that, you should run the `react-native link` to link your fonts to the iOS / Android native code